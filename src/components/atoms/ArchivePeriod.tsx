import styled from '@emotion/styled';
import { GRAY } from 'styles/colors';

const ArchivePeriod = styled.span`
  font-size: 32px;
  font-weight: bold;
  color: ${GRAY[1]};
  line-height: 44.8px;
`;

export default ArchivePeriod;
