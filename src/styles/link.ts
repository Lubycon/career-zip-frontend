import { css } from '@emotion/react';

const linkStyle = css`
  text-decoration: none;
  color: inherit;
  :hover,
  :focus,
  :active {
    text-decoration: none;
    color: inherit;
  }
`;

export default linkStyle;
